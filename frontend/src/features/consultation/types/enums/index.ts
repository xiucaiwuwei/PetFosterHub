// 问诊类型枚举
export enum ConsultationType {
  VIDEO = 'video',
  TEXT = 'text'
}

// 宠物类型枚举
export enum PetType {
  DOG = 'dog',
  CAT = 'cat',
  OTHER = 'other'
}

// 预约状态枚举
export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed'
}